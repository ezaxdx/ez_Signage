# 자체 시스템 다이어트 진단 — CLAUDE.md + 메모리 + /loop (2026-05-20)

**분류**: [검토 요청]
**작성**: 회사 PC Claude Code 자율 (v10.4 후속·재발 방지 점검)

## 1. 현재 상태 측정 (객관)

| 항목 | 측정값 | 권장 한도 | 상태 |
|---|---|---|---|
| `mice-design-guide/CLAUDE.md` 줄 수 | 926 | 60~150 (HumanLayer·DataCamp 검증) | ⚠️ **620% 초과** |
| CLAUDE.md 크기 | 48,602 bytes (~12K 토큰) | <5K 토큰 | ⚠️ **240% 초과** |
| CLAUDE.md 헤더 수 (#·##·###) | 113 | <30 | ⚠️ **377% 초과** |
| 메모리 파일 수 (`memory/*.md`) | 43 | <30 (1개월+ 활성) | ⚠️ **143% 초과** |
| §7 자기 점검 질문 | 23개 | 5~7개 (HumanLayer) | ⚠️ **329% 초과** |
| 자작 `/loop` 명령 | 1건 (사용자 명시 후 폐기 결정) | 0건 | ⚠️ 폐기 대기 |

> 측정 명령: `wc -l CLAUDE.md` · `grep -cE "^#{1,3} " CLAUDE.md` · `ls memory/*.md | wc -l`

## 2. 비대화 위험 분석

### 2.1. CLAUDE.md 926줄 + 113 헤더 = 룰 충돌·attention 분산
- HumanLayer 검증: "Frontier thinking LLMs can follow ~150-200 instructions with reasonable consistency"
- Claude Code system prompt 자체 ~50 instructions
- 사용자 CLAUDE.md 113 헤더 + 다수 룰 = 합산 200+ 초과 가능성
- 결과: 룰 일부 무시·자기 강화 루프·"영역" 어미 같은 단어 반복 증상

### 2.2. 메모리 43개 = 매 세션 로드 부담
- 5/16 자동 메모리 기능 도입 후 누적 증가
- 5/19 야간 자율 + 5/20 야간 = 신규 8건 추가 (35 → 43)
- 매 세션 인덱스 로드 = 토큰 부담 (인덱스만 ~150줄)

### 2.3. /loop 자작 명령 = 멈춤 학습 원인
- 5/19 박제 = 폐기 결정 (`feedback_loop_pattern_anti.md`)
- 단 실제 폐기·CLAUDE.md에서 /loop 룰 제거 X = 5/22 라이브 후 진행

## 3. 다이어트 우선순위 (5/22 라이브 후 적용 권장)

### 3.1. P0 — 즉시 효과 큰 다이어트
| 항목 | 작업 | 효과 |
|---|---|---|
| CLAUDE.md §7 자기 점검 23 → 7 질문 | 5/17~5/19 추가된 16개 점검을 메모리·외부 docs로 분리 | 매 답변 토큰 ~30% 절약 |
| `/loop` 관련 룰 제거 | CLAUDE.md §6 듀얼 Claude 관련 5 질문·자율 사이클 룰 폐기 | 멈춤 학습 차단 |
| §16-A~D 누적 결정 → decisions.md로 이관 | 5/14·5/16·5/19 사이클 결정 본문 → decisions.md 링크만 | CLAUDE.md ~300줄 감축 |
| 메모리 1개월+ 미수정 archive | 43건 중 grep 후 식별 | 인덱스 ~30% 감축 |

### 3.2. P1 — 점진 다이어트
| 항목 | 작업 | 효과 |
|---|---|---|
| §15 banner_gen_os 참조 = orphan 명시 (v4.1)·압축 | 100줄 → 20줄 요약 | 80줄 감축 |
| §16 화면 명세 → docs/IA_v4.md 인용으로 대체 | 본문 → 외부 docs 링크 | 150줄 감축 |
| 매월 1회 다이어트 점검 cron 도입 | scripts/diet_check.mjs | 자동 누적 회피 |

### 3.3. P2 — 구조 정합
| 항목 | 작업 | 효과 |
|---|---|---|
| 메모리 카테고리 폴더 분리 (feedback·project·reference) | memory/feedback/·memory/project/·memory/reference/ | 인덱스 가독성 |
| 동의어 alias 자동 검증 cron (X-배너 잔존 모니터) | scripts/aliases_drift_check.mjs | 재발 방지 |

## 4. 다이어트 후 목표

| 항목 | 현재 | 목표 (5/22 후 1주 안) |
|---|---|---|
| CLAUDE.md 줄 수 | 926 | 250~350 (60% 감축) |
| CLAUDE.md 헤더 | 113 | 40~50 |
| 메모리 active | 43 | 25~30 |
| §7 자기 점검 | 23 질문 | 7 질문 |
| `/loop` 룰 | 잔존 | 0건 |

## 5. 단어 반복 점검 (자모 발작 회피)

5/19 PO 명시 "영역같은거 더 문제 없게 하고" 후 자체 카운트.

| 단어 | CLAUDE.md 출현 | 권장 한도 | 대체 |
|---|---|---|---|
| "영역" | (별도 측정 필요) | 5회 이하 | 작업·범위·부분·곳 |
| "박제" | (별도 측정 필요) | 5회 이하 | 기록·저장·메모 |
| "정합" | (별도 측정 필요) | 5회 이하 | 일치·맞춤·확인 |
| "본업" | (별도 측정 필요) | 5회 이하 | 주 업무·본래 작업 |

→ 다이어트 작업 중 자체 카운트·대체 적용 의무.

## 6. /loop 폐기 절차 (5/22 라이브 후)

1. `feedback_loop_pattern_anti.md` 메모리 = 영구 박제 완료 ✅
2. CLAUDE.md §6 듀얼 Claude 자율 사이클 룰 = 폐기 (5 질문·8 영역·"통과 못 한 cycle = skip" 명시 모두 제거)
3. `automation/po_loop.sh` 스크립트 보존 (참조용)·실제 사용 X
4. `<<autonomous-loop-dynamic>>` 형식 사용 명시 (Anthropic 공식 sentinel)
5. 자율 작업 = "무한 명령 + Anthropic 지속성 문구" 패턴 (`feedback_autonomous_infinite_command.md`)

## 7. 권장 진행 시점

- **D-1 (5/21~5/22)**: 다이어트 X·안정 운영 우선
- **5/22 라이브 직후**: 회고만·다이어트 X
- **5/23~5/24**: 휴식·다이어트 X
- **5/25~5/29 (1주차)**: P0 다이어트 (자기 점검 23 → 7·`/loop` 폐기·decisions.md 이관)
- **5/30~6/5 (2주차)**: P1 다이어트 (외부 docs 분리·메모리 archive)
- **6/6+ (3주차+)**: P2 다이어트 (cron 도입·카테고리 분리)

## 8. 위험·주의

- CLAUDE.md 다이어트 시 = 5/14·5/19 sample 결정 본문 보존 (decisions.md로 옮기되 삭제 X)
- 메모리 archive 시 = 삭제 X·`memory/archive/2026-XX/` 이동 (1개월 후 재평가)
- /loop 폐기 시 = 사용자가 자율 시스템 정착 후만 (Stage 4·5/22 후 1개월+)
- 다이어트 후 즉시 = TSC·빌드·harness 회귀 점검

## 9. 자체 점검 — 본 보고서 자체

- [x] [검토 요청] 분류 첫 줄 명시
- [x] 모든 수치 객관 측정 (`wc -l`·`grep -c` 등)
- [x] "97%·완벽" 표현 0건
- [x] 다이어트 권장 = 5/22 라이브 후로 분리 (D-1 안정 우선)
- [x] 사용자 결정 영역 명시 (P0·P1·P2 단계별)
