# 관리자 페이지·데이터 학습 관리자 재설계 명세 (2026-05-13)

> 사용자(조기흠 사원, AXDX팀) 제공 명세 + 피드백 3건
> 환경장식물 v9.25 커밋·배포 완료 이후 적용 예정 (다음 사이클 v9.26+부터 진입)

---

## 1. 관리자 페이지 (`/admin`) — 운영 대시보드 + AI 관리

관리자 전용. 운영 현황 중심. **운영 KPI ↔ 전체 프로젝트 현황 통합** (사용자 피드백 반영).

### 1) 운영 KPI (상단 카드)

- 진행 중 프로젝트 수 (stage='in_progress' 누적)
- 이번 주 신규 프로젝트 수
- 이번 주 발주 완료 수 (finalized_at 기준)
- 발주서 완료율 (%) = finalized_items / total_items
- 추천 다운로드 전환율 (%) = 추천 → finalized 경로 비율
- AI 추천 정확도 (rolling, 목표 70%) — 신호등 색상

### 2) 유저 관리

- 활동 사용자 목록
- 권한 단계: 일반 사용자 / 관리자 (사내 SSO 연동 시 자동 매핑)
- ⚠️ **데이터허브 연동 결정 전 — 구현 보존 + 접근 차단**
  - 사용자 퍼널 분석 영역으로 활용 검토 중

### 3) 전체 프로젝트 현황 (운영 KPI와 통합)

- 필터: 파트 · PM · 행사장 · 기간 · 상태
- 테이블 16컬럼: NO · 파트 · PM · 행사 · 상태 · 발주일 · 컨펌율 · 항목수 · AI정확도 · 담당자 · 비고 · 편집 · 다운로드 · 승인 · 반려 · 보기
- 상태별 Stacked Bar (파트 × 진행·완료·반려)
- 이번 달 일정 (D-14 ~ 오늘 ~ D+7 캘린더 형태)

### 4) AI 관리 (신규 — 사용자 추가)

> 관리자가 AI 환경을 설정할 수 있는 영역

- Gemini API 호출 건수 · 토큰 사용량 · 일일/월간 누계
- 비용 추정 ($, ₩) · 한도 알림 (예산 임계값)
- 이상 사용자 알림 (예: 동일 항목 5회 이상 재호출)
- AI 환경 설정 (모델·프롬프트·임계값 등)

---

## 2. 데이터 학습 관리자 (`/admin/learning`) — 학습 + 마스터 데이터 통합

환경장식물 종류·동의어 등 마스터 데이터 이쪽으로 통합.

### 1) 상단 KPI 카드

- 행사장 — 학습된 행사장 누적 수 (venues 테이블 기준)
- 환경장식물 — 표준 종류 누적 수 (11종 + 신규)
- 동의어 — 매핑된 동의어 누적 수 (비표준 → 표준)

### 2) 개요

- 학습 누적 그래프 (월별·행사장별 추가 추이)
- 단계별 분포 (입력 10% / 중간 30% / 컨펌 70% / 완료 100%) 비율
- AI 추천 정확도 추이 (월 단위 rolling)

### 3) 행사장별 학습 현황

- 행사장 · 프로젝트 수 · 항목 수 · 단계별 분포 · 정확도 추정
- 정확도 40% 이하 행사장 강조 (학습 강화 대상)
- 행 클릭 시 해당 행사장 상세 (학습 데이터 · 시설 가이드 · 예외 패턴)

### 4) 행사장 (마스터 관리) — 단일 사이드바 버튼, 내부 6 서브

- 행사장 추가 폼 (이름 · 지역 · 종류 · 홀 분리 · 면적 · 천장고 · 도면 업로드)
- 신규 요청 대기 (사용자 신청 → 관리자 승인·반려)
- 도면 학습 큐 (Vision API 분석 상태: queued · processing · done · failed)
- 시설 가이드 (행사장별 카테고리 제약 · 주의사항 · 완성도)
- 예외 패턴 모니터 (venue+rule 3회 이상 위반 = 가이드 검토 필요 플래그)
- **수정 요청사항** (사용자 발견 오류·개선 제안 트래킹, 2026-05-13 사용자 추가)

### 5) 동의어 (마스터 관리) — 단일 사이드바 버튼, 내부 3 서브

- 비표준명 → 표준명 매핑 표 (검색 · 추가 · 삭제)
  - 예: '스프링배너' = 'X배너' / '롤업' = '롤업배너' 등
- **카테고리 권장 — 프로그램 파트 12종 × 환경장식물 매트릭스** (2026-05-13 사용자 명시)
  - 회의·전시·비즈니스 매칭·비즈니스 프로그램·공식행사·부대행사(공모전·체험·투어)·의전·등록·영접영송·**홍보** 각각에 매칭된 환경장식물 기본 세트
  - 어드민 편집·추가·삭제 가능
- 환경장식물 종류 관리 (표준 종류 · 규격 · 재질 · 레이아웃)

---

## 사용자 피드백 3건 (필수 반영)

| # | 피드백 | 처리 |
|---|---|---|
| ① | 운영 KPI ↔ 전체 프로젝트 현황 통합 | "운영 대시보드" 단일 화면 |
| ② | 유저 관리 | 구현 보존 + 접근 차단 (데이터허브 연동 후 활성) |
| ③ | AI 관리 추가 | 관리자가 AI 환경 설정 가능한 신규 섹션 |

---

## 첨부 디자인 시안 (사용자 제공 PNG 3건)

> 노션·메신저 첨부 — 본 docs에서는 URL 직접 접근 불가
> 실제 적용 시 시안 PNG 참조 필요

1. 관리자 페이지 — 운영 대시보드
2. 관리자 페이지 — AI 관리
3. 데이터 학습 관리자

---

## 사이클 계획 (v9.26+) — 5사이클 통합 완료 (2026-05-13)

| 사이클 | 작업 | 작업량 | 상태 |
|---|---|---|---|
| v9.26 | 운영 대시보드 (KPI + 프로젝트 현황 통합) | 중 | ✅ 완료 |
| v9.27 | AI 관리 페이지 신설 (Gemini 사용량·비용·한도·이상 알림 + 환경 설정) | 중~큼 | ✅ 완료 |
| v9.28 | 유저 관리 접근 차단 (RBAC 보강) | 작 | ✅ 완료 |
| v9.29 | /admin/learning 5개 섹션 재정렬·개요 섹션 신설 | 중 | ✅ 완료 |
| v9.30 | 시설 가이드·예외 패턴 모니터 KPI 카드 보강 | 작~중 | ✅ 완료 |

5사이클 모두 통합 브랜치 `auto/v9.26-v9.30-admin-redesign-260513`에 누적 후 한 번에 머지 (사용자 명시: "커밋이래 배포 끄응..." — 배포 1회).

### 결과 — 각 코드 위치
- `/admin` (v9.26): `app/(dashboard)/admin/page.tsx` + `AdminOpsClient.tsx`
- `/admin/ai` (v9.27): `app/(dashboard)/admin/ai/page.tsx` + `AdminAiClient.tsx`
- `/admin/users` (v9.28): `app/(dashboard)/admin/users/page.tsx`
- `/admin/learning` 재정렬 (v9.29): `app/(dashboard)/admin/learning/LearningManagerClient.tsx`
- 시설 가이드 KPI (v9.30): 같은 파일

### DB 마이그레이션 (PM 액션)
- `supabase/migration_v9_27_admin_ai_settings.sql` 실행 → admin_ai_settings 영구화 활성

### 검증
- TSC 0 에러 (사이클별 + 통합)
- Next 빌드 25/25 라우트 PASS

---

## v9.31 핫픽스 — 학습 관리자 댑스 정정 (2026-05-13)

### 사용자 지적
"데이터 학습 관리자 ... 에 맞지 않는데? 너 댑스 이해 못해?"

### 명세 §2 5 대섹션 IA ↔ 코드 댑스 매칭 표

| 명세 § | 대섹션 | 댑스 | 코드 구현 (v9.31) | 비고 |
|---|---|---|---|---|
| §2-1 | 상단 KPI 카드 | **상시 노출** (사이드바 X) | 페이지 상단 3카드 (학습된 행사장 / 환경장식물 종류 / 동의어 매핑) | 사이드바와 독립 |
| §2-2 | 개요 | 사이드바 1번 | `activeSection === 'overview'` | 학습 누적·단계별 분포·정확도 추이 |
| §2-3 | 행사장별 학습 현황 | 사이드바 2번 | `activeSection === 'venue-status'` | venue별 항목·정확도·40% 강조 |
| §2-4 | 행사장 | 사이드바 3번 (대섹션) | `activeSection === 'venues'` + 6 서브탭 | 단일 사이드바 항목 안 서브 통합 |
| §2-4-① | ㄴ 행사장 추가 폼 | 서브탭 (행사장 내부) | `venueSubTab === 'add'` | 이름·지역·종류·홀 분리·면적·도면 |
| §2-4-② | ㄴ 신규 요청 대기 | 서브탭 | `venueSubTab === 'requests'` | 승인·반려 |
| §2-4-③ | ㄴ 도면 학습 큐 | 서브탭 | `venueSubTab === 'queue'` | queued·processing·done·failed |
| §2-4-④ | ㄴ 시설 가이드 | 서브탭 | `venueSubTab === 'facility'` | 행사장별 카테고리·주의사항·완성도 |
| §2-4-⑤ | ㄴ 예외 패턴 모니터 | 서브탭 | `venueSubTab === 'exceptions'` | venue+rule 3회 이상 = 검토 플래그 |
| (+ 수정 요청) | ㄴ 수정 요청 | 서브탭 (보조) | `venueSubTab === 'corrections'` | FacilityGuidePanel에서 사용자 신고 |
| §2-5 | 동의어 | 사이드바 4번 (대섹션) | `activeSection === 'synonyms'` + 3 서브탭 | 단일 사이드바 항목 안 서브 통합 |
| §2-5-① | ㄴ 비표준 → 표준 매핑 | 서브탭 | `synonymSubTab === 'mapping'` | 검색·추가·삭제 |
| §2-5-② | ㄴ 카테고리 권장 | 서브탭 | `synonymSubTab === 'category'` | 행사 유형별 기본 세트 표 (v9.31 신규) |
| §2-5-③ | ㄴ 환경장식물 종류 관리 | 서브탭 | `synonymSubTab === 'types'` | 종류·규격·재질·레이아웃 |

### v9.29 (잘못된 평면화) ↔ v9.31 정정 비교

| 항목 | v9.29 (잘못) | v9.31 (정정) |
|---|---|---|
| 사이드바 항목 수 | 7개 (개요·행사장 학습 현황·행사장 관리·시설 가이드·수정 요청·동의어 매핑·환경장식물 종류) | 4개 (개요·행사장별 학습 현황·행사장·동의어) |
| 그룹 라벨 | 3그룹 (개요·행사장·동의어) | 없음 (4개 평면 + desc 부제) |
| 행사장 내부 서브 | 사이드바 항목으로 평탄화 (3개) | 행사장 클릭 시 상단 서브탭 6개 |
| 동의어 내부 서브 | 사이드바 항목으로 평탄화 (2개) | 동의어 클릭 시 상단 서브탭 3개 |
| KPI 카드 위치 | 페이지 상단 (정확) | 페이지 상단 유지 |
| 댑스 인식 | 행사장·동의어 안 서브가 사이드바 평면과 동급 = 학습 부담 | 대섹션 → 서브탭 자연 멘탈 모델 |

### 핫픽스 추가 변경 (천정배너·파트 매칭)

| 문제 | 위치 | 변경 |
|---|---|---|
| 천정배너 잔존 (회의 결정 ⑤ 위반) | `app/components/facility/FacilityGuidePanel.tsx:65` `getGuideUnknowns()` | `${category} — 사전 협의 후 발주 권장` → `${category}` (카테고리명만) |
| 파트 매칭 미작동 (RecommendInput 누락) | `lib/ai/recommendSignage.ts` | RecommendInput.programParts 필드 + Gemini 프롬프트 [프로그램 파트 매핑] 블록 + 후처리 program_part 자동 채움 |
| 파트 매칭 미작동 (case-a 입력 X) | `app/(dashboard)/projects/new/case-a/page.tsx` | 파트 다중선택 UI + API body 추가 + 엑셀 파트 컬럼 + emerald 배지 |
| 파트 매칭 미작동 (NewProjectButton DB) | `app/(dashboard)/dashboard/components/NewProjectButton.tsx` | design_items INSERT에 part·program_part 자동 채움 (pickPartForFormat) |

---

## 진입 전제

- 환경장식물 v9.21~v9.25 main 머지·배포 완료 ✅ (2026-05-13)
- 계약서 사전자료수집 보고 완료 후 진입 권장
- 사용자 명시 컨펌 후 사이클별 진입
