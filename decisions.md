# 의사결정 로그

> 작은 결정도 누적. "왜 X를 쓰는가?" 질문 시 검색.
> CLAUDE.md를 부풀리지 않으면서 일관성 유지.

## 형식

```
## YYYY-MM-DD — <결정 제목>
**컨텍스트**: <어떤 상황에서 결정이 필요했는가>
**선택**: <무엇을 골랐나>
**대안**: <고려한 다른 옵션>
**이유**: <왜 그걸 골랐나>
**되돌릴 수 있는가**: <쉬운가, 어려운가>
```

## 2026-05-14 — AI 환경 설정 ↔ step별 페르소나 중복 제거 (v9.48)

**컨텍스트**: 사용자(조기흠 사원, AXDX팀, 2026-05-14) 지적 — `/admin/ai` 화면에 ′AI 환경 설정′(전역 폼, v9.27) + ′AI 추천 파이프라인 — step별 페르소나 설정′(v9.46) 두 영역이 모델·Temperature·system_prompt를 중복 노출. "이게 뭐가 다른데?" 의문. 또한 amber 안내·footer가 코드 메타(`lib/ai/agentPipeline.ts`·`localStorage(admin_ai_settings_v2)`·`admin_ai_settings 테이블`·`/projects/new/case-a` 등)를 그대로 노출 → 사용자 ′응?′ 룰(코드 표현이 보고/UI에 노출되면 안 됨) 위반.

**선택**:
  1. **두 안내 문구 완전 삭제** — step별 폼 위 amber 박스(′현재 사이클은 Gemini 2.5 Flash/Pro만 실제 호출... 후속 사이클에서 어댑터 도입... 기본 파이프라인 본문(`lib/ai/agentPipeline.ts`)이 그대로 사용...′) + AI 환경 설정 아래 footer(′localStorage(`admin_ai_settings_v2`) 저장... admin_ai_settings 테이블로 영구화 예정...′) 제거. 사용자에게 의미 없는 코드 메타 정보.
  2. **전역 ′AI 환경 설정′ 폼 슬림화** — 6개 입력칸(모델·Temperature·최대 출력 토큰·월 예산·이상 사용자 임계값·시스템 프롬프트) → 3개(최대 출력 토큰·월 예산·이상 사용자 임계값) 만 유지. 모델·Temperature·system_prompt는 step별 폼으로 일원화. 제목 ′AI 환경 설정′ → ′운영 설정 — 호출 한도·예산′으로 변경(의미 명확화). `AiSettings` 인터페이스에서도 model/temperature/system_prompt 필드 제거.
  3. **step별 폼 라벨 친절화** — ′현 사이클은 Gemini만 활성′ → ′현재 Gemini 사용 중′ / ′페르소나 (system_prompt)′ → ′페르소나′ / ′기본 본문:′ → ′기본 동작:′ / ′비우면 기본 본문 사용′ → ′비워두면 기본 동작 그대로′. 보고자 친화 표현으로 일관 정리.
  4. **(추가, 사용자 명시) step별 페르소나 영역 ′어디서 쓰이는지′ 안내 박스** — blue 박스 1줄 ′적용 위치: 새 프로젝트 만들기 → AI 추천 받기′. 사용자가 step 카드를 보고 "그래서 저게 어디서 쓰이는데?" 의문 즉시 해소.

**대안**:
  - 전역 폼을 그대로 유지 + amber 안내만 부드럽게 변경 (중복 의문 해소 안 됨)
  - 전역 폼은 ′default fallback′ 라벨로 명시 + step별 폼은 ′override′ 라벨로 명시 (UI 복잡도 증가, 사용자 ′응?′ 추가 발생 위험)
  - `AiSettings` 인터페이스의 model/temperature/system_prompt 필드를 코드에 보존하되 UI에서만 제거 (dead 코드 누적 — recommendSignage.ts에서 전혀 참조하지 않음)

**이유**:
  - **단일 진실 소스**: 모델·Temperature·system_prompt 설정은 step별 폼만 있어야 사용자 멘탈 모델 단순. 전역 폼은 ′운영 정책′(호출 한도·예산·알림)에만 집중.
  - **fallback 동작 보존**: recommendSignage.ts에서 step별 오버라이드(`stepOverrides`)가 비어 있으면 PIPELINE_BLOCKS의 기본 body로 자동 fallback (이미 `applyPersonaOverrides`·`buildPipelineLogicSectionWith` 구현 완료). 따라서 전역 폼에서 모델·Temperature·system_prompt 입력칸을 제거해도 추천 호출은 정상 동작.
  - **코드 메타 노출 = 사용자 ′응?′ 룰 위반**: `lib/ai/agentPipeline.ts`·`localStorage`·`admin_ai_settings 테이블`·`/projects/new/case-a` 같은 코드 표현은 보고/UI에 부적합. 사용자(조기흠 사원, 비개발자) 입장에서 의미 없는 정보.
  - **친절화 라벨**: ′system_prompt′·′기본 본문′ 같은 코드 표현 → ′페르소나′·′기본 동작′ 같은 보고자 친화 표현. 입력 필드 hint·placeholder도 동일 정신.

**보존**:
  - AiPipelineCard.tsx 미삭제 (v9.45 사용자 명시 보존 조건)
  - v9.46 step별 페르소나 기능 본체 그대로 (lib/ai/agentPipeline.ts + STEP_SETTINGS_KEY localStorage)
  - DB 스키마 변경 0건 / 의존성 추가 0건
  - recommendSignage.ts의 step별 + 전역 fallback 동작 유지 — step에서 비어있으면 PIPELINE_BLOCKS 기본 동작 자동 적용
  - 토큰·비용 통계(상세 사용량 4카드 + 예산 사용률 + 30일 추이 + 이상 사용자 알림 표) 모두 그대로 보존

**되돌릴 수 있는가**:
  - 안내 문구 1·2: 매우 쉬움 — Edit 1회로 amber 박스/footer 복원
  - 전역 폼 슬림화: 쉬움 — `AiSettings` 인터페이스에 model/temperature/system_prompt 필드 + DEFAULT_SETTINGS의 기본값 + UI 입력칸 4개(모델·Temperature·system_prompt textarea) 복원
  - 라벨 친절화: 매우 쉬움 — 라벨 문자열 4개만 환원

**상세**: PROGRESS.md 2026-05-14 (v9.48)

---

## 2026-05-14 — IA SOT(김연아 대리님 노션 스크린샷) 정렬 — 운영/AI/학습 3영역 (v9.47)

**컨텍스트**: 사용자(조기흠 사원, AXDX팀, 2026-05-14) 명시 — "코드를 IA에 맞춰서 커밋". IA SOT = 김연아 대리님이 노션(https://www.notion.so/35f485898ea180919bc8ca6ef9a53967)에 올린 스크린샷 3장. v9.46에서 AI 페르소나 per step 기능을 추가한 직후, v9.39~v9.46 동안 누적된 KPI 카드들이 IA와 미세 차이가 있어 일괄 정렬.

**선택**:
  1. **운영 대시보드 KPI 5종 → 4종** — IA 목표(진행 / 신규(최근 일주일) / 전체 / 완료율)에 맞춰 정렬. ′이번 주 발주 완료′ → ′전체 프로젝트′(누적 카운트, 의미 변경). ′추천 다운로드 전환율′ 카드 삭제(IA 미포함 sales funnel 지표). ′이번 주 신규′ 기준을 weekStart(이번 주 일요일)에서 7일 전(sevenDaysAgo)으로 변경 — IA 명시 ′최근 일주일′ 의미와 정합.
  2. **AI 관리 KPI 3종 라벨 변경** — 토큰·비용 카드 → AccuracyTable 평균값을 ′ai 추천 정확도′ 단일 카드(3 sub-label: 행사장 / 파트별 / 도면(커밍순))로 통합. ′이번 달 호출′ → ′총 API 호출 수′(누적, ′총′ 의미 정확화). ′이번 달 비용′ → ′이상 사용자 알림′(임계값 초과 사용자 카운트, 신규). 토큰·비용 통계는 하단 ′상세 사용량′ + ′예산 사용률′ 영역에 그대로 보존(예산 임계 경고 연계 유지).
  3. **데이터 학습 관리자 사이드바** — IA 목표(6 평탄: 행사장 학습 현황 / 행사장 관리 / 환경장식물 종류 / 동의어 매핑 / 시설 가이드 / 수정요청)와 v9.36 코드 SECTIONS가 100% 일치 검증 완료. 추가 변경 없음. dead state(VenueSubKey·SynonymSubKey 타입, venueSubTab·synonymSubTab state, VENUE_SUBTABS 상수)만 단순화로 정리.

**대안**:
  - **영역 1**: 5종 그대로 유지 (IA 미정합) / 4종 + 전환율을 추천 정확도로 대체 (v9.45에서 이미 /admin/ai로 이관된 항목 — 운영 대시보드 중복 위험)
  - **영역 2**: 토큰·비용 카드 그대로 + ′이상 사용자 알림′만 추가 (4번째 카드 — IA가 ′3종′으로 명시) / 정확도 단일 평균만 표시(IA 명시 3 sub-label 누락)
  - **영역 3**: ′개요′·′프로그램 파트′ 메뉴 복구 (사용자 명시 ′시안 100% 매칭′ 위반) / 사이드바 그대로 두고 dead state도 보존(코드 위생)

**이유**:
  - IA SOT는 김연아 대리님(검토자) 명시 결정사항. 코드와 IA 차이가 누적되면 다음 화면 추가 시 기준점 모호.
  - 영역 1: 운영 대시보드의 ′발주 완료′·′전환율′은 v9.39 이후 사용자가 일주일에 한 번만 본다는 시그널 누적. 평균 보기 빈도가 낮은 카드는 슬림 4종으로 압축.
  - 영역 2: ′토큰·비용′은 관리자가 매일 보는 정보가 아님. ′ai 추천 정확도′는 사용자(조기흠 사원)가 핵심 KPI로 명시 (메모리 [[feedback-incremental-accuracy]] 정합). ′이상 사용자 알림′은 데이터허브 SSO 연동 전 단계에서 admin이 모니터링해야 할 보안 시그널.
  - 영역 3: v9.36에서 이미 IA에 부합하게 6 평면 메뉴로 정착. dead state 정리는 위생 작업 — 향후 사이드바 추가 시 SectionKey union 재작성 부담 감소.

**보존**:
  - AiPipelineCard.tsx 미삭제 (v9.45 사용자 명시 보존 조건)
  - v9.46 AI 페르소나 per step 기능 그대로 (lib/ai/agentPipeline.ts + STEP_SETTINGS_KEY localStorage)
  - DB 스키마 변경 0건 / 의존성 추가 0건
  - ′프로그램 파트′ 코드는 메뉴에서만 빼고 lib/programParts.ts·case-a·NewProjectButton 사용처 그대로
  - 토큰·비용 통계 (하단 ′상세 사용량′ + ′예산 사용률′ + ′최근 30일 호출 추이′)
  - ′프로그램 파트′ / ′개요′ 섹션 코드 (`false &&` 가드)

**되돌릴 수 있는가**:
  - 영역 1: 쉬움 — KpiData에 thisWeekFinalized·conversionRate 다시 추가 + grid-cols-4 → 5 + page.tsx 계산 복원
  - 영역 2: 쉬움 — Kpi3 인터페이스 복원 + AccuracyRowMini 컴포넌트 제거 + KPI 카드 영역 v9.46 코드로 환원
  - 영역 3: 매우 쉬움 — 주석만 변경 + dead state는 어차피 미사용이라 복원 불필요

**상세**: PROGRESS.md 2026-05-14 (v9.47)

## 2026-05-13 — 학습 관리자 사이드바 5 메뉴: 프로그램 파트 신규 + 동의어 → 환경장식물 (v9.32)

**컨텍스트**: 사용자(조기흠 사원, AXDX팀, 2026-05-13) 명시 — "개요·행사장별 학습 현황·행사장·프로그램 파트·환경장식물" 5 메뉴 + 옵션 A "환경장식물 메뉴 클릭 → 종류 관리 + 동의어 매핑 + 카테고리 권장". v9.31 4 메뉴(개요·행사장 학습 현황·행사장·동의어)에서 ′동의어′ 메뉴를 ′환경장식물′로 명칭 변경하고 4번에 ′프로그램 파트′ 신규 메뉴 삽입.

**선택**:
  1. SectionKey union 4개 → 5개: `'overview' | 'venue-status' | 'venues' | 'program-parts' | 'synonyms'`
  2. 사이드바 5 메뉴 순서(사용자 명시 그대로): 개요 → 행사장별 학습 현황 → 행사장(6 서브 유지) → **프로그램 파트(신규)** → **환경장식물(구 동의어, 3 서브 그대로)**
  3. 프로그램 파트(4번) — `lib/programParts.ts`의 PROGRAM_PARTS 12종을 PROGRAM_PART_GROUPS 3 그룹(프로그램 8 / 참가자 응대 3 / 홍보 1)으로 카드 그리드 표시. 각 카드는 코드·이름·hint·PROGRAM_PART_SIGNAGE_HINTS 매핑 ID 배지. read-only (편집·추가·삭제는 추후 사이클).
  4. 환경장식물(5번) — 사이드바 라벨만 ′동의어′ → ′환경장식물′ + icon `ImageIcon`으로 변경. SectionKey 값은 `'synonyms'` 유지 (synonymSubTab 키와 분기 로직 보존 → 코드 영향 최소화).

**대안**:
  - SectionKey 값까지 `'signage'`로 변경 (synonymSubTab 등 모든 분기 변수명 일괄 변경 필요 — v9.32 사이클에 부담)
  - 프로그램 파트를 ′환경장식물′ 하위 서브탭으로 배치 (사용자 명시 "사이드바 5 메뉴" 의도 위반)
  - 프로그램 파트 편집 UI까지 같이 작업 (한 사이클 범위 초과)

**이유**:
  - 사용자 명시 5 메뉴 순서·메뉴명·옵션 A 그대로 반영
  - SectionKey 키 ′synonyms′ 유지로 v9.31의 synonyms·synonymSubTab 분기 100건+ 코드 보존 (라벨만 변경 = 위험 최소)
  - 프로그램 파트 read-only로 시작 — NewProjectButton·case-a에서 사용 중인 PROGRAM_PARTS 12종 SOT를 어드민에서 시각 확인 가능 (v9.31에서 파트 매칭 흐름 작동 후 자연 보강)

**되돌릴 수 있는가**: 쉬움 — SECTIONS 배열에서 `program-parts` 1개 항목 제거 + 5번 라벨을 ′동의어′로 환원 + program-parts 렌더링 블록 1개 삭제하면 v9.31 동작.

**상세**: PROGRESS.md 2026-05-13 (v9.32)

## 2026-05-13 — 학습 관리자 사이드바 4 대섹션 + 내부 서브탭 통합 (v9.31)

**컨텍스트**: 사용자 강한 지적 — "데이터 학습 관리자 ... 에 맞지 않는데? 너 댑스 이해 못해?". v9.29의 7개 평면 사이드바가 명세 §2 5 대섹션 IA(KPI + 개요 + 행사장별 학습 현황 + 행사장(5서브) + 동의어(3서브))와 불일치.

**선택**:
  1. 사이드바 = 4 대섹션 (KPI 카드는 상시 상단 노출 → 사이드바 X)
  2. ′행사장′ 대섹션 클릭 시 상단 6 서브탭 네비 (추가/요청대기/큐/시설가이드/예외패턴/수정요청)
  3. ′동의어′ 대섹션 클릭 시 상단 3 서브탭 네비 (매핑/카테고리권장/환경장식물종류)
  4. 기존 facility-guides·correction-requests·signage-types 블록은 venues/synonyms 서브탭 조건으로 흡수 — 코드 재작성 최소화
  5. 사이드바 4개 버튼은 desc 부제(예: "추가 / 신규 요청 대기 / 도면 학습 큐 / 시설 가이드 / 예외 패턴")로 서브탭 내용 미리보기

**대안**:
  - v9.29 평면 유지 (사용자 거부)
  - 5 대섹션 모두 사이드바 (KPI도 사이드바 → 명세 §2-1과 모순 — KPI는 상시 노출이라 사이드바 항목 X)
  - 사이드바 + 우측 사이드바 2단계 (UI 복잡도 증가, 명세 단순 IA 의도와 불일치)

**이유**: 명세 IA는 "대섹션 안에 서브"가 자연 멘탈 모델. 사이드바 평탄화는 ′행사장′ 안 5개와 ′동의어′ 안 3개를 사용자가 같은 댑스로 인식하게 만들어 학습 부담 증가. 서브탭 패턴은 venues·synonyms 진입 후 컨텍스트 유지하면서 미세 분리 가능.

**되돌릴 수 있는가**: 쉬움 — SECTIONS 배열을 7개로 다시 풀어쓰고 venueSubTab·synonymSubTab 분기 제거하면 v9.29 동작.

**상세**: PROGRESS.md 2026-05-13 (v9.31)

## 2026-05-13 — 파트 → 환경장식물 자동 매칭 흐름 작동 (v9.31)

**컨텍스트**: 사용자 강한 지적 — "행사 만들 때 파트 자동 적히는 거 그 파트별로 적용되는 사항 왜 적용 안 됨?". 진단 결과:
  - NewProjectButton: programParts 다중선택은 UI formats[fid].selected만 갱신. design_items INSERT에 part·program_part 미주입.
  - recommendSignage.ts: RecommendInput에 programParts 필드 자체가 없음 → Gemini 프롬프트에 파트 컨텍스트 주입 안 됨.
  - case-a: 파트 선택 UI 없음. 엑셀 ′파트′ 컬럼 빈값.

**선택**:
  1. lib/programParts.ts 헬퍼 3종 신설 — pickPartForFormat (FORMAT_PRESETS id 기반) / partsForFormat (역방향) / programPartName (코드→한글)
  2. recommendSignage.ts:
     - RecommendInput에 programParts?: string[] 필드 추가
     - RecommendItem에 program_part·program_part_name 추가
     - SYSTEM_INSTRUCTION에 "각 항목에 매칭된 파트 1개 명시" 지시
     - userText에 [프로그램 파트 매핑] 블록 자동 주입 (선택 파트별 권장 환경장식물 ID 표시)
     - 응답 후처리: AI program_part 검증 → 실패 시 partsForFormat ∩ selectedParts 첫 매치로 자동 채움
  3. case-a 페이지에 프로그램 파트 다중선택 UI (PROGRAM_PARTS 12종 그룹별 체크박스, emerald 강조)
  4. case-a·NewProjectButton 양쪽에서 design_items INSERT 시 part: program_part_name, program_part: code 채움
  5. DB 컬럼 호환성: program_part 컬럼 없을 경우 재시도 (마이그레이션 미적용 환경 보호)
  6. UI 시각화: case-a review에 emerald 파트 배지 + 엑셀 ′파트′ 컬럼 자동 채움

**대안**:
  - AI 응답에만 의존 (Gemini가 약속 안 지킬 위험 — JSON 누락·잘못된 코드)
  - 사용자 수동 입력 컬럼만 유지 (사용자 검토 부담 ↑, 자동화 4필터 ④ 위반)
  - PROGRAM_PART_SIGNAGE_HINTS만 사용 + EVENT_TYPE_RECOMMEND 통합 (goals/current.md에서 사용자가 추진 안 함으로 표시)

**이유**: 사용자 명시 회의 컨셉(1차 파트별 + 2차 행사장별) 구현. 자동화 4필터 ④ — 파트 한 번 선택하면 추천 결과 + 엑셀까지 일관 적용. AI 응답이 약속 안 지킬 때 후처리로 안전망. 단계별 fallback으로 마이그레이션 미적용 환경에서도 동작.

**되돌릴 수 있는가**: 쉬움 — recommendSignage.ts 후처리 program_part 블록 + NewProjectButton matchedPartCode 라인 + case-a programParts state 제거하면 원복. lib/programParts.ts 헬퍼는 그대로 두어도 부작용 없음.

**상세**: PROGRESS.md 2026-05-13 (v9.31)

## 2026-05-13 — 시설 가이드 미확인 항목 추가 강조 텍스트 제거 (v9.31)

**컨텍스트**: 사용자 지적 — 천정배너(행잉)에 "사전 협의 후 발주 권장" 잔존. 회의 결정 ⑤ "추가 강조 텍스트 X" 위반.

**선택**: `getGuideUnknowns()`에서 `items.push(\`${category} — 사전 협의 후 발주 권장\`)` → `items.push(category)` 단순화. 카테고리명만 노출.

**대안**:
  - 강조 라벨을 다른 톤으로 변경 (회의 ⑤ 완전 부합 안 됨)
  - 미확인 항목 섹션 자체 제거 (구체화 가능한 정보는 유지하는 게 사용자 도움)

**이유**: 회의 결정 ⑤ + v9.21 강조 라벨 톤다운 원칙과 일치. 추가 안내는 install_allowed의 note에서 이미 노출되므로 중복.

**되돌릴 수 있는가**: 매우 쉬움 — 한 줄 원복.

**상세**: PROGRESS.md 2026-05-13 (v9.31)

## 2026-05-07 — v4.1 대전환: "디자인 의뢰" → "리스트 가이드"

**컨텍스트**: 회의(260507) — "디자인 자체는 걷어내고 학습 + 추천부터" 전면 방향 전환
**선택**:
  1. 명칭 변경: "MICE 제작물 디자인 의뢰 가이드" → **"제작물 리스트 가이드"** (UI/문서/PPT 파일명만)
  2. 캔버스 편집기·시안 업로드·AI Gemini 추천: orphan (코드 보존, 진입점만 차단)
  3. 행사 유형 단일 → 프로그램 파트 다중선택 (EZ 폴더링 40.04~40.20)
  4. 행사장 자유 텍스트 → 드롭다운 + 신규 요청 모달
  5. 데이터 학습 관리자 (/admin/learning) 신설 — 행사장·도면 학습
  6. 동선배너: 룰베이스 (인원/200 + 홀분리 + 주출입구 + 대형행사장 가산)
**대안**: 캔버스 편집기 유지 + AI 추천 정확도 보강
**이유**: 회의 결정 — 디자인 시안 자체는 외주 디자이너 영역, 본 앱은 "표준화된 추천 리스트 + 발주 가이드" 역할. 학습 데이터 누적 후 정확도 확보 우선.
**되돌릴 수 있는가**: 쉬움 (orphan 코드 보존 + program_parts_legacy 컬럼 백업 + repo·DB·env 명칭 유지)

## 2026-05-07 — 자동 누적 학습 사이클 (사용자 핵심 지시)

**컨텍스트**: "앞으로 제작되는 모든 행사는 기존 정보에 자동 업데이트 되어야만함"
**선택**: 별도 트리거 없이 projects INSERT → liveStats 5분 캐시 만료 후 자동 재집계
**대안**: 명시적 학습 큐 / 사용자가 "학습시키기" 버튼 클릭
**이유**: 자동화 4필터 ④ 사람 개입 줄이기 — 별도 행위 없이 데이터 자체가 누적되도록
**되돌릴 수 있는가**: 쉬움 — liveStats.ts 그대로

## 2026-05-07 — 행사장 단위: 건물 기본 + 홀 분리 옵션

**컨텍스트**: "코엑스 전시장 vs 코엑스 컨퍼런스홀 차이" 처리 방식
**선택**: 건물 단위 기본 + 등록 시 "홀 단위 분리" 체크박스 (venue_halls 테이블)
**대안**: 무조건 홀 단위 / 무조건 건물 단위
**이유**: 모든 행사장이 홀 분리 안 됨. 도면 가진 곳만 세분화 가능하게 옵션화.
**되돌릴 수 있는가**: 쉬움 — venue_halls는 venues에 종속, 미사용해도 영향 X

## 2026-05-07 — 엑셀 "파트" 컬럼 = design_items.program_part 한글 자동 매핑 (질문 4 답)

**컨텍스트**: 명령본 §G-1 17컬럼 표 "파트(program_part 한글)" 매핑 규칙 결정 필요
**선택**:
  - design_items.program_part text 컬럼 신규 (단일값, nullable, 다중일 경우 "40.04,40.19" 쉼표 구분)
  - 엑셀 출력 시 코드 → PROGRAM_PARTS 한글로 자동 변환 (코드 표시 X)
  - null이면 design_items.part(legacy 자유입력) fallback → 둘 다 null이면 빈칸
  - 매핑 실패 시 console.warn (학습 데이터 누락 추적)
  - 시트 정렬: 파트 한글 가나다순 → 종류명 가나다순
**대안**: project.program_parts(text[])를 행마다 같은 값 표시 / 매핑 X
**이유**: PDF 가이드 1행이 "한 환경장식물의 용도"를 사람이 읽을 수 있게 표기. 코드 표기는 디자이너·발주처 의미 X. 행별 다른 파트 가능해야 정합성.
**되돌릴 수 있는가**: 쉬움 — fallback 로직만 제거

## 2026-05-07 — setup_date / teardown_date 폼 제거 (질문 5 답)

**컨텍스트**: 명령본 §3-3 "추천에 직접 안 쓰면 폼에서도 제거" 정합성 적용
**선택**:
  - **유지** (폼 + 출력): attendees_count(동선배너 룰), event_language(엑셀 "언어")
  - **제거** (UI만): setup_date, teardown_date — 추천 X, 출력 X
  - DB 컬럼은 nullable 유지 (drop 금지) — 기존 데이터 보호 + v5 운영 캘린더 복귀 가능
**대안**: 4개 다 제거 / 4개 다 유지
**이유**: 회의 결정 + 사용자 답변 — 추천/출력 사용 여부가 기준. setup·teardown은 운영용. 캘린더 기능은 v5에서 별도.
**되돌릴 수 있는가**: 쉬움 — UI만 제거, 컬럼 보존

## 2026-05-07 — orphan 코드 보존 정책 (시안 업로드 / 캔버스 / AI / 사외 공유)

**컨텍스트**: v4.1 대전환에서 4개 영역(시안 업로드 일괄·품목별 / 캔버스 편집기 / AI Gemini 추천 / 사외 공유 링크)을 걷어냄
**선택**:
  - **코드는 보존** (delete 금지) — 진입점만 차단
  - 보존 위치: 기존 경로 그대로 (lib/ai/recommendSignage.ts / projects/[id] 캔버스 / NewProjectButton step3 시안 / share/[token])
  - **v5 복귀 가능**: 사용자 피드백 누적 후 ① 시안 업로드 다시 켤지 ② 캔버스 편집기 부활할지 ③ AI 추천 정확도 측정 후 재가동할지 단위별 결정
**대안**: 완전 삭제 → 향후 복귀 시 git history에서 부활 (히스토리 추적 어려움)
**이유**: 회의 결정대로 일단 빼되, 학습 데이터 누적 후 정확도 확보되면 즉시 복귀할 수 있도록 안전망 유지
**되돌릴 수 있는가**: 매우 쉬움 — 헤더 메뉴 + 진입 버튼만 다시 노출

## 2026-05-07 — Vision API 호출 시점: 다음 사이클

**컨텍스트**: 도면 학습 트리거 시 실제 Vision API 호출 여부
**선택**: 이번 사이클은 learning_jobs INSERT만 (스켈레톤)
**대안**: 즉시 Vision 호출 + 결과 저장
**이유**: Vision 비용·실패 처리·프롬프트 튜닝까지 한 사이클에 끼면 다른 단위가 밀림. 룰베이스 동선배너로 우선 동작 확인 후 도입.
**되돌릴 수 있는가**: 쉬움 — recommendDongseonBannerFromFloorPlan() 시그니처는 미리 정의됨

## 2026-04-24 — 캔버스 렌더러: Fabric.js v5

**컨텍스트**: 제작물 미리보기 + 슬롯 드래그 위치 조정 구현 필요
**선택**: Fabric.js v5
**대안**: Konva.js, plain Canvas API
**이유**: IText 더블클릭 직접 타이핑, object:modified 이벤트, 좌표 % 변환이 충분히 검증됨
**되돌릴 수 있는가**: 어려움. Fabric.js 의존 코드가 캔버스 전체에 퍼져 있음.

## 2026-04-24 — 좌표계: 0~100% 정규화

**컨텍스트**: Gemini bbox(0~1000)와 캔버스(px) 사이 변환 필요
**선택**: 앱 내부는 0~100% 정규화, PPT 출력 시 inch로 변환
**대안**: 0~1000 그대로 유지
**이유**: CSS % 값과 직접 연동 가능, 규격(mm) 변경에 무관
**되돌릴 수 있는가**: 가능하나 전체 좌표 코드 수정 필요.

## 2026-05-07 — 행사장 배치도 업로드 위치: 프로젝트 생성 시

**컨텍스트**: 향후 AI가 배치도 이미지를 분석해 signage 설치 위치·품목 추천 예정
**선택**: 프로젝트 생성 위자드에 배치도 이미지 업로드 필드 추가 (선택 사항)
**대안**: /info 페이지에서 나중에 업로드 / 별도 배치도 전용 페이지 신설
**이유**: PM이 프로젝트 생성 시점에 이미 행사장 도면을 가지고 있음. 초기에 받으면 AI 추천 단계가 빨라짐.
  입력 형태: case-c (시안 이미지 업로드)를 배치도도 받도록 확장하거나, 1단계 위자드에 optional 필드 추가.
  분석 흐름: 배치도 이미지 → Gemini 분석 → 구역별 signage 타입·규격 추천 → design_items 일괄 생성
**되돌릴 수 있는가**: 쉬움. optional 필드이므로 기존 흐름 무중단.

## 2026-05-07 — 대시보드 4-case: 선택지가 아닌 입력 옵션 설명

**컨텍스트**: 대시보드에 4가지 시작 방법 카드 표시 구현
**선택**: 4가지를 별도 경로가 아닌 "같은 시작, 선택 가능한 입력 옵션" 시각화로 변경. 단일 CTA 버튼.
**대안**: 4개 별도 링크로 /projects/new/case-a~d 분기
**이유**: 사용자 피드백 — "4가지 선택이 아닌 같은 시작이지만 입력 할수도 안할수도 있는 형태"
**되돌릴 수 있는가**: 쉬움. /projects/new/case-* 페이지는 그대로 존재.

## 2026-05-07 — AI 엔진: Anthropic Claude → Google Gemini

**컨텍스트**: 사용자가 Gemini API 키 직접 제공, "AI는 AIzaSy... 를 쓰자"
**선택**: Gemini 2.5 Flash REST API 직접 호출 (의존성 추가 없이 fetch만)
**대안**: @google/generative-ai SDK 설치 / Anthropic 유지
**이유**: 응답 속도(한국어 자연어 처리 빠름), `responseMimeType: 'application/json'` 강제로 파싱 안정성, 신규 의존성 추가 없음
**되돌릴 수 있는가**: 쉬움 — `recommendSignage.ts` 한 파일만 수정. Anthropic 코드는 git history에 보존.

## 2026-05-07 — 시드 데이터 단일 소스: dashboardSeed.ts

**컨텍스트**: 명세 6번(AI 사전교육 자료) 정의를 코드에 어떻게 둘 것인가
**선택**: `lib/data/dashboardSeed.ts` 한 파일에 환경장식물·동의어·행사이력·PM·발주처·재질·실측분석 모두 임베드
**대안**: Supabase 테이블 + DB 시딩 / 분산 JSON 파일
**이유**: 1단계는 데이터 양이 적음 (행사 54·실적 17·동의어 10) → 마이그레이션 부담 없이 즉시 사용. 분석 자동화가 진입할 때 Supabase로 이관.
**되돌릴 수 있는가**: 가능하지만 Supabase 이관 시 RLS·권한 설계 필요.

## 2026-05-07 — AI 추천에 과거 유사 행사 컨텍스트 자동 주입

**컨텍스트**: 명세 5번 — "과거 유사 행사 데이터 참조 → 제작물 리스트 추천, 맥락 유지"
**선택**: `findSimilarPastEvents()` 가중치 점수 (행사장 +4, 발주처 +5, 분류 +3, PM부서 +2) → 상위 5건을 Gemini 프롬프트에 자동 주입
**대안**: 사용자 수동 선택 / 임베딩 기반 RAG / 단순 카테고리 매칭
**이유**: 17건 시드 규모에 RAG는 과함. 단순 가중치로 충분히 의미 있는 컨텍스트 확보. 데이터 누적되면 임베딩으로 전환.
**되돌릴 수 있는가**: 쉬움 — 함수 호출만 제거.

## 2026-05-07 — 신규 프로젝트 위자드 4단계 → 3단계

**컨텍스트**: 사용 목적 단계가 행사 유형(event_type)과 기능적으로 중복. 사용자 피드백 — "사용 목적 삭제(행사 유형과 기능 동일)"
**선택**: step 2(사용 목적)를 통째로 제거. 시안 업로드는 step 3(제작물 선택)로 이동
**대안**: 사용 목적을 행사 유형과 분리 유지 / 한 화면에 합치되 두 필드 모두 입력
**이유**: 행사 유형 선택 시 권장 환경장식물 자동 체크(명세 6.2.6)로 대체 가능 → 사용 목적은 별도 입력 가치 낮음
**되돌릴 수 있는가**: 쉬움 — 코드는 `{false && step === 2 && ...}`로 비활성만 시켜 향후 즉시 복구 가능

## 2026-05-07 — 행사 유형 → 환경장식물 자동 체크

**컨텍스트**: 명세 6.2.6 "행사분류 선택 시 해당 환경장식물 자동 선택 상태로 제공" 구현 필요
**선택**: `EVENT_TYPE_RECOMMEND` 매핑 (행사 유형 9종 → 권장 환경장식물 IDs) → 클릭 시 `setFormats`로 자동 체크
**대안**: AI 추천 결과로 대체 / 사용자 수동 선택만
**이유**: 사용자 검토 부담을 늘리지 않고 (자동화 4필터 ④번) 출발점만 제시. 사용자는 체크박스 추가/제거로 미세 조정만.
**되돌릴 수 있는가**: 쉬움 — `selectEventType` 함수만 제거.

## 2026-04-24 — DB: Supabase PostgreSQL + RLS

**컨텍스트**: 멀티유저 실시간 협업, 프로젝트별 접근 제어 필요
**선택**: Supabase (PostgreSQL + Realtime + Storage + Auth)
**대안**: Firebase Firestore (banner_gen_os 프로토타입에서 사용)
**이유**: RLS로 row-level 보안, item_contents Realtime 구독, 추후 데이터허브 SSO 연동 고려
**되돌릴 수 있는가**: 어려움. 마이그레이션 비용 큼.

## 2026-05-11 — 행사장별 학습 데이터를 시스템의 핵심 입력으로 확정

**컨텍스트**: 1차 AI 시험 결과 — 도면+메타만으로는 천정배너 등 부속 카테고리 추천 불가. 같은 행사장 과거 사례(WSCE 2021 등) 학습이 정확도의 핵심.
**선택**: 행사장별 학습 데이터(`venue_specs` + `ceiling_banner_patterns` 등)를 시스템의 1차 입력으로 채택. 도면·시설개요는 보조.
**대안**: 도면+행사 메타로만 추천 / 룰베이스 단독 유지
**이유**: 1차 시험에서 학습 없이 추천한 천정배너 카테고리 누락 = 수량 -20%. 같은 행사장 과거 패턴이 있어야 부속 시설(라운지·컨퍼런스장 천정) 인지 가능.
**되돌릴 수 있는가**: 쉬움 — `findSimilarPastEvents` 가중치 조정만으로 학습 비중 낮출 수 있음.
**상세**: `docs/VENUE_LEARNING_INSIGHTS_260511.md` 참조

## 2026-05-11 — 모르는 카테고리는 추천 안 함 (환각 방지)

**컨텍스트**: AI가 학습 데이터에 없는 카테고리도 일반 패턴으로 추천하려는 경향 위험. 천정배너 사례.
**선택**: 학습 데이터에 없는 카테고리는 `[추천 없음 + 매뉴얼 보강 권고]`로 자동 표기. 추측 금지.
**대안**: 학습 부족해도 일반 컨벤션 패턴으로 추정 추천
**이유**: 자동화 4필터 ④번 — 사람 검수 부담 줄이는 게 핵심. 환각 1건이 사람 검수 비용 5건 추가시킴.
**되돌릴 수 있는가**: 쉬움 — `recommendSignage.ts`의 fallback 로직만 변경

## 2026-05-11 — 매뉴얼 PDF 한국어 OCR 자동화 필요 확정

**컨텍스트**: 1차 시험에서 킨텍스 매뉴얼 PDF의 한국어 폰트 깨짐으로 천정·행잉·하중 섹션 미추출. pdftotext 한계 확인.
**선택**: Tesseract 한국어 OCR을 학습 데이터 파이프라인에 추가. 매뉴얼 PDF + 결과보고서 PDF 모두 OCR 통과 후 시드에 반영.
**대안**: 매뉴얼은 사람이 직접 입력 / OCR 없이 진행
**이유**: 매뉴얼은 행사장별 모든 표준 규격의 1차 출처. OCR 자동화 없으면 학습 데이터 확보가 행사장당 1일+ 소요.
**되돌릴 수 있는가**: 쉬움 — OCR 모듈만 분리

## 2026-05-11 — 입력 데이터 축적: 단계별 분리 + 신뢰도 가중치

**컨텍스트**: 2차 피드백 회의에서 "출력 완료본만 학습이 아닌 입력 결과도 축적" 요구. 그러나 입력·중간 수정 값은 미완성이라 그대로 학습 시 추천 품질 저하 우려가 현장에서 제기됨.
**선택**: 데이터의 ′확정 단계′에 따라 분리 저장 + 신뢰도 가중치 차등 적용.
  - ① 입력 단계: 가중치 10% (집계 통계만)
  - ② 중간 수정: 30% (수정 패턴 추출)
  - ③ 사용자 컨펌: 70% (항목별 추천 가중치)
  - ④ 발주·다운로드 완료: 100% (정답 풀)
  - 시설 가이드 검증 통과: +20% 보너스
**대안**:
  - 모든 입력을 동일 가중치로 학습 (품질 저하 위험)
  - 출력 완료본만 학습 (윗선 요구 미충족)
**이유**: 윗선 요구사항(입력 축적)을 수용하되, 미완성 데이터 혼입으로 인한 추천 품질 저하를 분리 저장·가중치로 차단. 학습 풀별 영향도 정량 추적 가능 → 입력 학습의 효과·위험도 모니터링 가능.
**되돌릴 수 있는가**: 쉬움 — 가중치 상수만 조정 (예: 모든 단계 0으로 두면 출력 완료본만 학습하던 기존 동작과 동일)
**상세**: `docs/91.2_AI업무파트너기획_제작물_리스트_가이드_260511.md` §11-2

## 2026-05-11 — 시스템 최종 목적: 발주·설치 가능한 형태 안내

**컨텍스트**: 2차 피드백 — "리스트 가이드의 최종 목적이 결과물 생성 자체가 아니라 실제 발주와 설치가 가능한 형태로 안내하는 것".
**선택**: 출력물을 단순 리스트 → ′발주 의뢰서′ 형식으로 디벨롭. 시설 가이드(리깅·하중·디지털 사이니지) 자동 검증 + 컨펌·발주·설치·철거 마감일 자동 안내(별도 레이어).
**대안**: 단순 리스트만 출력, 발주 형식은 사용자 수동 작성
**이유**: 사람이 계속 변경되어 발주 행위 자체의 품질이 낮은 현행 문제 해결. 시스템으로 발주 품질을 보장.
**되돌릴 수 있는가**: 쉬움 — 출력 컬럼·안내 레이어만 변경

## 2026-05-11 — 센터별 시설 가이드 사전 학습 데이터 신설

**컨텍스트**: 행사장별 설치 가능 여부(리깅·하중·디지털 사이니지)·금지조건이 매뉴얼에 묻혀있어 매번 사람이 확인.
**선택**: `venue_facility_guide` 시드 신설. 행사장별로 ① 설치 가능 항목 ② 리깅 가능 여부·하중 제한 ③ 시설별 주의사항·금지조건 ④ 디지털 사이니지 허용 범위 ⑤ 센터 사이트·매뉴얼 참고 URL을 정형화.
**대안**: 매뉴얼 OCR 결과만 그대로 사용 (정형화 없이)
**이유**: 매뉴얼은 행사장마다 형식이 다름. 정형화된 시드 데이터로 추천 시 자동 검증 가능. ′설치 불가′ 항목을 추천 단계에서 차단.
**되돌릴 수 있는가**: 쉬움 — 시드 데이터 삭제로 검증 비활성화 가능

## 2026-05-11 — 시설 가이드 위반은 안내 알랏 (강제 차단 아님)

**컨텍스트**: 2차 피드백에서 시설 가이드(리깅·하중·디지털 사이니지 규정) 학습이 추가됐는데, 위반 시 어떻게 동작할지 선택 필요.
**선택**: **안내 알랏 1회 + 사용자 ′그래도 진행′ 선택권 보장**. 강제 차단 안 함.
  - 위반 입력 시 알랏 표시: "매뉴얼 기준 표준 규격은 X입니다. 입력값은 표준 외입니다. ※ 시스템 정보가 오래돼 틀릴 수 있음."
  - 사용자가 [수정하기] 또는 [그래도 진행] 선택
  - ′그래도 진행′ 항목은 비고에 `[시설 가이드 외] 사용자 확인 진행` 자동 표기
  - ′예외 케이스′ 태그로 학습 누적 → 같은 행사장에 예외가 잦으면 매뉴얼 데이터 갱신 권고 자동 알림
**대안**:
  - 강제 차단 (입력 자체 거부)
  - 알랏 표시 없이 그냥 통과
**이유**:
  - 시스템 학습 정보가 오래되어 틀릴 가능성 — 강제 차단 시 사용자가 진행할 방법이 없어짐
  - 사용자가 직접 행사장에 문의해 예외 허가 받은 경우 등 사용자가 더 많은 정보를 가질 수 있음
  - ′최소 안내′ 역할 + 예외 누적 학습으로 매뉴얼 데이터 점진 갱신 가능
**되돌릴 수 있는가**: 쉬움 — 알랏 모드 플래그(`facility_check_mode = warn | block | off`) 한 곳만 변경
**상세**: `docs/91.2_AI업무파트너기획_..._260511.md` §11-6

## 2026-05-11 — 학습 대상 폴더 명확화

**컨텍스트**: 2차 피드백 — "센터별(장소별) 환경장식물 리스트와 시설 가이드를 함께 반영 (사전 학습)"의 학습 대상 폴더가 모호.
**선택**: 학습 대상은 **`참고자료/환경장식물 행사별/` + `참고자료/도면/` 폴더 전체**. 학습 기준은 **′각 행사별로 환경장식물을 어떤 형식으로 제공하는가의 패턴′**.
**대안**: 일부 핵심 행사만 / 새 학습 폴더 별도 생성
**이유**: 137개 행사 폴더·30개 행사장·14개 발주엑셀이 이미 정리돼 있음. 추가 폴더 생성 없이 즉시 활용. 행사별 형식 패턴이 추천 결과의 기준값.
**되돌릴 수 있는가**: 쉬움 — 학습 폴더 경로만 변경
**상세**: `docs/VENUE_EVENT_MATRIX_260511.md` (자동 추출 결과)

## 2026-05-11 — 시스템 핵심 동작 목표: 점진적 정확도 + 장소+행사 선택 시 누적 데이터 AI 주입 (v9)

**컨텍스트**: 윗선 명문화 — "기존+신규 앱 사용 데이터 누적으로 점차 정확해지는 게 목표. 사용자가 장소+행사 유형을 고르면 해당 누적 정보를 모두 AI에 줘서 최종 출력을 받는다."
**선택**:
  1. `lib/ai/accumulatedContext.ts` 신설 — Supabase에서 venue 매칭 프로젝트 5건의 design_items를 단계별(입력 10%·중간 30%·컨펌 70%·완료 100%) 가중치로 압축
  2. `lib/ai/recommendSignage.ts` 자동 주입 — seed + venueSignageHelper + accumulatedContext 3종을 단일 Gemini 프롬프트로 통합
  3. 가중치 70%(컨펌) 이상 항목은 ′정답에 가까움′ 태그 → AI가 우선 반영
  4. 별도 학습 트리거 없음 — `confirmed=true` 또는 `finalized_at` 컬럼만 채우면 다음 추천부터 자동 누적
**대안**:
  - 수동 학습 큐 (사용자가 "이 프로젝트 학습시키기" 클릭)
  - 완료된 프로젝트만 학습 (입력·중간 단계는 제외)
  - 임베딩 기반 RAG
**이유**:
  - 자동화 4필터 ④ — 사람 개입 없이 자동 누적 (별도 행위 추가 X)
  - 단계별 가중치 분리로 미완성 데이터의 학습 풀 혼입 위험 차단 (v3 결정 연장선)
  - RAG는 현재 누적량(17건+) 규모에 과함. 가중치 합산으로 충분히 의미 있는 컨텍스트 확보
**되돌릴 수 있는가**: 쉬움 — `STAGE_WEIGHT`만 `{input:0, mid:0, confirmed:0, finalized:100}`으로 두면 ′완료본만 학습′ 모드로 회귀
**상세**: `docs/91.2_AI업무파트너기획_..._260511.md` §14-A

## 2026-05-11 — 헤더 1차안 17컬럼 복원 + 편집 화면 4컬럼 기본 숨김

**컨텍스트**: 윗선 — "1차안 헤더로 돌리라". 현재 운영 헤더는 1차안과 컬럼 일부 차이. 1차안 원본: `구버전/참고자료/환경장식물 최종 정보 모음/환경장식물/제작물 디자인 의뢰 가이드 결과물 안.xlsx`.
현장 우려: 17컬럼 전부 편집 화면에 노출하면 사용자(특히 사원급)가 어지러움.
**선택**:
  1. 17컬럼 헤더로 복원 (NO·파트·구분·장소·사용 목적·품목·언어·규격·재질·수량·내용·비고·담당자·디자인업체·출력업체·설치시간·철거시간)
  2. **편집 화면 초기 숨김 4개**: 디자인업체·출력업체·설치시간·철거시간 (`show_in_editor=false` 기본값)
  3. **최종 다운로드 시 자동 노출**: 엑셀 17컬럼 모두 포함. 사용자가 의식하지 않아도 자동.
  4. **PPT 제외 3개**: 담당자·디자인업체·출력업체 (`include_in_ppt=false` 기본값). PPT는 14컬럼.
  5. **컬럼별 토글**: `show_in_editor` / `include_in_excel` / `include_in_ppt` 3종 플래그. 사용자가 UI에서 ON/OFF 가능, 소프트 토글로 데이터 보존.
  6. **DB 신설**: `project_column_settings(project_id, show_in_editor, include_in_excel, include_in_ppt jsonb)` 프로젝트별 설정 저장.
**대안**:
  - 모든 컬럼 항상 표시 (사원급 사용자 부담 큼)
  - 4컬럼 영구 제거 (윗선 요구 미충족)
**이유**: 윗선 요구(17컬럼 복원)와 현장 우려(편집 부담)를 모두 충족. 사용자는 편집 단계에서는 핵심 12~13컬럼만 보고, 발주 단계에서 자동으로 17컬럼 엑셀이 생성됨. 컬럼 토글로 사용자별·프로젝트별 맞춤 가능.
**되돌릴 수 있는가**: 쉬움 — 기본값 JSON만 수정하면 ON/OFF 상태 일괄 변경 가능. 컬럼 자체는 항상 DB에 존재(소프트 토글).
**상세**: `docs/91.2_AI업무파트너기획_제작물_리스트_가이드_260511.md` §14

## 2026-05-13 — 어드민 운영 대시보드: KPI ↔ 프로젝트 현황 단일 화면 통합 (v9.26)

**컨텍스트**: 사용자 피드백 ① "운영 KPI랑 전체 프로젝트 현황이 나눠질 건 아닌 것 같아서 조율했습니다". 기존 IA 초안은 운영 KPI / 유저 관리 / 전체 프로젝트 / AI 사용량 4섹션 분리.
**선택**: `/admin` 신규 라우트에 KPI 6카드 + 16컬럼 프로젝트 테이블 + 파트 Stacked Bar + 캘린더를 단일 페이지로 통합. 기존 `/data` (13탭 분석 자료)는 별도 진입으로 보존. 헤더 "관리자 페이지" 링크 `/data` → `/admin` 변경.
**대안**: ① 4섹션 그대로 유지 (사용자 거부) ② `/data`에 KPI 위쪽 추가 (13탭 페이지가 너무 무거워짐) ③ 별도 `/admin/ops` 라우트 (URL 일관성 깨짐 — `/admin`을 hub로 사용하는 게 직관)
**이유**: 사용자 명시 피드백. 운영 현황과 프로젝트 상세는 같은 의사결정 단위(파트·PM·행사장) 기준이라 분리 시 컨텍스트 손실. 단일 화면 + 필터로 같은 정보를 다양한 각도로 볼 수 있음.
**되돌릴 수 있는가**: 쉬움 — `/admin/page.tsx` 삭제하고 헤더 링크 `/data`로 복귀하면 끝.
**상세**: PROGRESS.md 2026-05-13 (v9.26)

## 2026-05-13 — AI 관리 영역: 모델·프롬프트 설정을 관리자에게 노출 (v9.27)

**컨텍스트**: 사용자 피드백 ③ "관리자 페이지의 AI는 우리가 설정할 수 있도록 환경을 구축하는 것이라고 생각해서 내용을 추가". 기존엔 단순 사용량 통계만 계획됨.
**선택**: `/admin/ai` 페이지에 사용량 4카드 + 비용 추정(USD+KRW) + 월 예산/임계 경고 + 30일 추이 + 이상 사용자 알림 + **AI 환경 설정 폼**(모델·temperature·max_output_tokens·system_prompt) 추가. 1차는 localStorage 저장, 다음 사이클에 admin_ai_settings 테이블로 영구화.
**대안**: ① 사용량 통계만 (피드백 ③ 미충족) ② 즉시 DB 영구화 (마이그레이션 풀 사이클·RLS 검증 부담) ③ recommendSignage.ts에 즉시 주입 (현재 사이클은 UI 우선)
**이유**: 사용자가 관리자 입장에서 비용·정확도 트레이드오프를 직접 조정 가능해야 함. localStorage는 안전한 1차 저장소(서비스 사이드 영향 없음). DB 영구화는 admin_ai_settings 마이그레이션 SQL만 별도 작성하고 다음 사이클에 활성.
**되돌릴 수 있는가**: 쉬움 — `/admin/ai` 페이지 삭제. localStorage 키 admin_ai_settings_v1만 비우면 됨.
**상세**: PROGRESS.md 2026-05-13 (v9.27) + supabase/migration_v9_27_admin_ai_settings.sql

## 2026-05-13 — 유저 관리: 구현 보존 + UI 접근 차단 (v9.28)

**컨텍스트**: 사용자 피드백 ② "유저관리는,, 아직 데이터허브 연동이 결정되지 않아서 구현은 그대로 놔두되 들어갈 수 없도록 막아주세요". + "여기서 사용자 퍼널을 볼지 생각해보려고 합니다."
**선택**: `/admin/users` 라우트에 "준비 중" 화면 신설. 헤더의 유저 관리 링크는 cursor-not-allowed로 시각적 disabled. 기존 profiles 테이블·isAdmin·fetchUserRole 코드는 보존. 데이터허브 연동 결정 후 화면 본체만 교체하면 활성화.
**대안**: ① 라우트 자체를 만들지 않음 (헤더 메뉴와 불일치) ② middleware에서 강제 차단 (관리자도 disabled — 향후 활성 시 복잡) ③ admin 외 사용자 차단만 (피드백 ② 미반영)
**이유**: 사용자가 "구현 보존" + "접근 차단" 둘 다 명시. 페이지 자체는 admin 진입 시에도 "준비 중" 화면을 보여줘 데이터허브 연동 결정 시점까지 자리잡기 가능. 헤더 표시는 향후 활성 시점에 메뉴 위치를 미리 알리는 역할.
**되돌릴 수 있는가**: 매우 쉬움 — `/admin/users/page.tsx`를 실제 유저 관리 화면으로 교체하면 즉시 활성.
**상세**: PROGRESS.md 2026-05-13 (v9.28)

## 2026-05-13 — /admin/learning 5섹션 IA 재정렬 + 개요 신설 (v9.29)

**컨텍스트**: 명세 §2 5섹션 IA — 1) KPI 카드 2) 개요 3) 행사장별 학습 현황 4) 행사장 마스터 5) 동의어. 기존 사이드바는 6개 섹션이 한 평면에 나열되어 명세 IA와 그룹 매핑이 안 됨.
**선택**: 사이드바를 그룹 라벨 3개("개요"·"행사장"·"동의어")로 분리. '개요' 섹션을 신규 추가(기본 진입점). 행사장 그룹에 venues + facility-guides + correction-requests 묶음. 동의어 그룹에 synonyms + signage-types 묶음.
**대안**: ① 6개 그대로 (피드백 IA 불일치) ② 5개로 강제 압축 (페이지 단위가 너무 커짐 — 마스터/주의사항/예외 패턴 등 의미적으로 분리되어야 함) ③ 탭 형식 (사이드바 일관성 깨짐)
**이유**: 명세 IA를 유지하면서 기존 페이지(코드량 1300줄)를 재작성하지 않고 그룹 라벨로만 시각 분리. 사용자의 멘탈 모델(개요 → 행사장 마스터 → 동의어)을 따라 진입 동선 자연스러움.
**되돌릴 수 있는가**: 쉬움 — SECTIONS 배열의 group 키 제거 + 단일 평면 nav 복원.
**상세**: PROGRESS.md 2026-05-13 (v9.29)

## 2026-05-13 — 시설 가이드·예외 패턴 KPI 카드 보강 (v9.30)

**컨텍스트**: 명세 §2-4 "시설 가이드 (행사장별 카테고리 제약 · 주의사항 · 완성도)" + "예외 패턴 모니터 (venue+rule 3회 이상 위반 = 가이드 검토 필요 플래그)". 기존 표 형태로만 노출되어 현장 의사결정자(어드민)가 한눈에 파악하기 어려움.
**선택**: 시설 가이드 섹션 상단에 KPI 4카드 추가(등록 행사장 + 5/6↑ 카운트 / 카테고리 제약 누계 / 주의사항 누계 / 평균 완성도 %). 예외 패턴 섹션 상단에 KPI 3카드 추가(전체 / 검토 필요(≥3회) / 완료 누계).
**대안**: ① KPI 없이 표만 유지 (요약 인지 부담) ② 별도 페이지 분리 (페이지 수 증가) ③ KPI를 페이지 최상단에 모음 (섹션 단위 의미 손실)
**이유**: 한눈에 검토 필요 여부를 시그널화. "완료 누계"는 가이드보다 신뢰도 높은 검증값(decisions.md 2026-05-12 "제작 완료 > 시설 가이드")이라 별도 카드로 강조.
**되돌릴 수 있는가**: 매우 쉬움 — KPI 카드 JSX 블록 두 곳만 제거.
**상세**: PROGRESS.md 2026-05-13 (v9.30)

## 2026-05-13 — 발주엑셀 venue 라벨 노이즈 필터 정제 (v9.25)

**컨텍스트**: 사용자 요청 "환경장식물 더 수정해야하는 부분 있는지 확인 및 수정". `_venue_signage_map.json`을 점검한 결과 venue 라벨 다수가 실제 행사장이 아닌 행사 내부 위치(`"등록데스크"·"통역부스"·"의무실"·"창고"·"방한용품배포대"·"운영요원 대기실"·"인천공항"·"차량"`) 또는 노이즈(`"-"·"미상"·"기타"`)로 등록됨. `computeMapCoverageByVenueKey()`·`buildCoverageForUnregisteredVenue()`가 이 노이즈를 그대로 fuzzy 매칭 후보로 사용해 오탐 가능 (예: 사용자가 "인천" 검색 시 "인천공항" 노이즈와 매칭).

**선택**:
  1. `VENUE_LABEL_NOISE_EXACT` 5개 + `VENUE_LABEL_NOISE_PATTERNS` 정규식 18종 신설
  2. `isVenueLabelNoise(label)` 헬퍼 — exact + pattern + 1자 이하 차단
  3. signageCategoryStandards.ts 두 곳의 venue 필터를 통일 (기존 `v !== '미상' && v !== '-'` → `!isVenueLabelNoise(v)`)
  4. 노이즈 라벨은 학습 매칭 대상에서 제외 (denied 처리 아님, 무시)

**대안**:
  - `_venue_signage_map.json` 원본 데이터 수정 (다음 파싱 시 다시 노이즈 유입)
  - 발주엑셀 원본을 다시 파싱하면서 venue 컬럼 사전 정제 (작업량 큼, 다른 사이클)
  - findVenueKey 매칭에 의존 (findVenueKey가 null 반환하면 자연 차단되지만, fallback 함수가 별도로 노이즈를 후보로 사용)

**이유**:
  - 사용자가 `goals/current.md`에서 G1·G3·G4를 "⛔ 추진 안 함"으로 표시한 상황에서, 데이터 정합성 측면 갭만 보강하는 안전한 작업
  - 노이즈 필터는 사용자 의사결정·정답지 편향과 무관한 순수 데이터 위생 작업
  - 향후 파싱 결과에 노이즈가 추가돼도 패턴 1곳만 확장하면 됨 (한 곳 유지보수)
  - learnings.md "정답지 노출 편향" 원칙 — 노이즈로 학습 있다고 표기되는 오탐 = 정답에 과한 신뢰 부여

**되돌릴 수 있는가**: 매우 쉬움 — `isVenueLabelNoise(v)` 호출을 다시 `v !== '미상' && v !== '-'`로 되돌리면 v9.23 동작.

**상세**: PROGRESS.md 2026-05-13 (v9.25)

## 2026-05-13 — 행사 장소 권역 라벨: 한국 행정구역 표준(광역시·도) (v9.24)

**컨텍스트**: 조기흠 사원 피드백 — "새 프로젝트 만들기에서 행사 장소를 지방이라고 하지 말고 도 이름으로 수정". 기존 5종 분류("서울"·"수도권"·"지방"·"제주"·"해외")는 두루뭉술하고 외부 발주처·디자이너에게 노출되는 라벨이라 정확한 행정구역 명칭 필요.
**선택**: `VenueRegion` union을 한국 행정 표준 18종으로 확장 — 광역시 8(특별시·광역시·특별자치시) + 도 9(도·특별자치도) + 해외. `REGION_ORDER` 상수로 광역시 → 도 → 해외 순서 통일. VENUE_LIST 22개 베뉴 region 재할당("지방" 4건·"수도권" 2건·"서울" 12건·"제주" 2건 → 정확한 광역/도 이름). VenueRequestModal·LearningManagerClient의 REGIONS 상수가 동일하게 REGION_ORDER 사용 → 사용자가 18개 중 선택.
**대안**:
  - 5종 그대로 유지 (사용자 명시 거부)
  - 광역시 8 + 권역 5(수도권·강원·충청·호남·영남·제주) 절충안 — 권역 묶음은 행정 표준 아님
  - DB region 컬럼 값까지 마이그레이션 (기존 데이터 손상 우려, 코드 라벨만 변경 후 점진 적용이 안전)
**이유**:
  - 외부 발주처·디자이너에게 노출되는 라벨이라 행정 표준 명칭이 신뢰도 높음
  - 사용자 명시 요청 + 메모리 [[feedback-doc-style-corrections]]: 두루뭉술 표현 회피
  - 18종 옵션은 드롭다운 optgroup으로 그룹핑되어 UX 부담 없음 (광역시 8개는 항상 상단 노출)
**되돌릴 수 있는가**: 쉬움 — VenueRegion union과 VENUE_LIST 22개 값만 5종으로 되돌리면 끝. DB는 영향 없음.
**상세**: PROGRESS.md 2026-05-13 (v9.24)

## 2026-05-13 — 시설 가이드 미등록 행사장도 발주엑셀 fallback으로 학습 커버리지 산출 (v9.23)

**컨텍스트**: v9.22의 `computeVenueCategoryCoverage()`는 venueFacilityGuide 5개(킨텍스·코엑스·송도·ICC·DDP)만 처리. SEED_PERFLIST 17건 중 나머지 12+개(롯데호텔·평창 알펜시아·그랜드하얏트·웨스틴조선·aT센터·OSCO·광주 등)는 findVenueKey가 null 반환 → coverage·후처리 모두 스킵. 미학습 카테고리가 노출되지 않아 사용자가 "이 행사장에서 AI가 정답을 안다"고 오해할 위험.

**선택**:
  1. **`buildCoverageForUnregisteredVenue(venueName)` 신설** — findVenueKey 매칭 실패해도 `_venue_signage_map.json` 단독으로 venue 이름 fuzzy 매칭 후 카테고리 학습 추출. 매칭 안 되면 6대 전부 미학습으로 정직 표기.
  2. **`resolveCoverageForVenue(venueName)` 통합 헬퍼** — 등록(venueFacilityGuide) 케이스는 computeVenueCategoryCoverage에서, 미등록은 buildCoverageForUnregisteredVenue로 분기. recommendSignage 후처리·formatCoverageForPrompt·LearningManagerClient 세 곳에서 동일 헬퍼 사용.
  3. **미등록 행사장 venue_key는 `unregistered::<slug>` 형식** — UI에서 식별 가능 + DB 충돌 방지. RecommendResult.coverage.venue_key 타입은 string으로 유지.
  4. **formatCoverageForPrompt에 미등록 안내 주석 자동 부착** — "※ 이 행사장은 시설 가이드 미등록. 발주엑셀 기록만으로 추출한 학습 데이터입니다."
  5. **case-a UI 시각화 동시 적용** — amber 안내 박스(coverage.missing.length>0) + 항목별 no_data_flag 배지 + amber 배경. 백엔드 보정이 사용자 화면에 노출되도록.

**대안**:
  - findVenueKey에 키워드 12개 더 추가 (롯데호텔·평창 등) — 시설 가이드 본문이 없는데 venue_key만 발급하면 다른 코드(buildVenueProfile·getVenueSpecs)에서도 매칭되어 정보 없는데 매칭됨 표시 발생
  - 미등록 행사장은 coverage 자체를 안 만듦 (v9.22 동작) — 사용자에게 "AI가 안전한가" 정보 없음
  - 시설 가이드 6개 신규 추가 — 한 사이클에 풀어야 할 작업이 너무 큼 (행사장당 30~50줄 시드 + max_width/standard_width 채움)

**이유**:
  - 사용자 명령 "미학습 부분과 추가 학습 필요한 부분 꼼꼼히 확인" — 시설 가이드 미등록 행사장 미처리는 v9.22의 명확한 갭
  - 발주엑셀 통합 맵에 평창 알펜시아·코엑스·킨텍스 등 13건의 실제 발주 기록 존재 → 정답에 가까운 신호
  - 사용자가 "이 행사장은 학습 데이터 부족"을 알면 매뉴얼 확인·운영팀 문의 등 추가 행동 취할 수 있음 (자동화 4필터 ④ "사람 검토 부담 줄이기" — 알게 만들면 일찍 처리)

**되돌릴 수 있는가**: 매우 쉬움 — `buildCoverageForUnregisteredVenue`만 제거하면 v9.22 동작으로 복귀. `resolveCoverageForVenue`는 venueKey null 시 null 반환하도록 변경만 하면 됨.

**상세**: PROGRESS.md 2026-05-13 (v9.23) / learnings.md 2026-05-11 "학습 데이터에 없는 카테고리는 솔직히 누락 표기"

## 2026-05-13 — 6대 표준 카테고리(외벽·게이트·가로등·X배너·천정·부속시설) 학습 분류 체계 채택

**컨텍스트**: goals/current.md 1단계 TODO "카테고리별 학습 항목 표준화" — 시드 데이터·시설 가이드·AI 추천·학습 관리자 4영역에서 카테고리명이 자유 문자열(예: '가로현수막 (외벽)', 'X-배너', '천정배너 (행잉)')로 표기되어 일관 분류·매칭 불가. AI 추천이 학습 없는 카테고리를 "그럴듯하게" 추측하는 환각 위험 (learnings.md 2026-05-11 정답지 노출 편향).

**선택**:
  1. **6대 표준 카테고리 키 신설** — outer_wall·gate·streetlight·x_banner·ceiling·support. 우선순위 1(외벽·천정) 2(게이트·가로등) 3(X배너·부속) 차등.
  2. **자동 분류 함수** `classifyCategory(text)` — 자유 문자열을 표준 키로 fuzzy 매칭 (키워드 기반). 카테고리 매칭 우선순위: gate → ceiling → streetlight → outer_wall → x_banner → support
  3. **행사장별 커버리지 자동 계산** `computeVenueCategoryCoverage()` — venueFacilityGuide.install_allowed + SEED_CEILING_BANNER_PATTERNS 두 시드를 합쳐 행사장별 6대 카테고리 학습 보유 여부 계산. denied 상태도 학습 데이터로 인정 (불가도 정보).
  4. **AI 추천 후처리 강제** — Gemini가 반환한 quantity>0 항목 중 미학습 카테고리(단 x_banner·support는 거의 모든 행사장 allowed이므로 제외)는 `[추천 없음 — 학습 데이터 부재]` rationale 강제 prepend + no_data_flag=true. 사용자가 발주 결정.
  5. **학습 관리자 시각화** — admin/learning 행사장 학습 현황 표에 카테고리 학습 컬럼 추가. emerald(학습)/slate(미학습)/rose(우선순위1 누락) 3색.

**대안**:
  - 11종 환경장식물 그대로 사용 (입자가 너무 작아 행사장별 부족 분야 식별 어려움)
  - 자유 문자열 그대로 두기 (현행 — 매칭 불가능)
  - 미학습 카테고리에 quantity=0 강제 (학습 시도 자체가 끊김 — 사용자가 처음 시도하는 행사장에서 항상 빈 추천)

**이유**:
  - 6개 그룹이 발주 실무자가 머릿속에서 정리하는 단위와 일치 (메모리 project-environment-decoration-ai-logic 파트별 추론 흐름과 동일)
  - x_banner·support는 거의 모든 행사장 공통 — no_data_flag에서 제외하면 일반 룰 추천 유지하면서 학습 우선 분야(외벽·천정·게이트·가로등)만 보강 신호 강조
  - quantity 보존 + 깃발만 세움 = 사용자 결정권 유지 (decisions.md 2026-05-11 "위반은 안내 알랏, 강제 차단 아님" 원칙과 정합)

**되돌릴 수 있는가**: 쉬움 — STANDARD_CATEGORIES 키워드 조정·classifyCategory 우선순위 변경만으로 분류 결과 변경. recommendSignage 후처리 블록을 if(false)로 감싸면 후처리 비활성화.

**상세**: PROGRESS.md 2026-05-13 (v9.22) 섹션 / learnings.md 2026-05-11 "학습 데이터에 없는 카테고리는 솔직히 누락 표기"

## 2026-05-13 — 시설 가이드 패널: 객관 정보 잘림 금지 + 강조 라벨 톤다운

**컨텍스트**: 환경장식물 시설 가이드 피드백 회의(260513) — 사용자 인용:
  - "두 번씩 눌러야 들어가지", "한 번 눌러서 들어가는 게 맞다"
  - "지금 다 잘리잖아요. 볼 수가 없잖아", "이 시설 가이드가 우리가 참고할 수 있는 코엑스에서 주는 객관적인 정보"
  - "운영팀 확인 필수 이런 건 필요 없어요. 일반적으로 이때 하더라라는 정보만"
  - "미확인 항목 ... 어떤 설치 품목·조건이 필요한지가 같이 리스트업이 돼야겠죠. 못 하겠다 하면 그냥 삭제"
  - "AI가 작성한 거라 점 찍고 그냥 이렇게 쭉 가잖아요. 보기 쉽게만"

**선택**:
  1. **한 번 클릭 진입 보장** — outer onClick={onClose} 제거. pointer-events 분리 + backdrop만 onClose. 패널 본체 stopPropagation으로 자식 클릭 격리.
  2. **호버 잘림 금지** — install_allowed note의 `truncate max-w-[180px]` 제거. break-words + whitespace-pre-wrap로 전체 노출. warnings·special_notes 동일.
  3. **AI 텍스트 가독성** — `lib/text/normalizeAiText.ts`의 `formatNoteText()` 한 곳에서 처리. 마침표 후 줄바꿈 삽입. case-a rationale 적용.
  4. **강조 라벨 톤다운** — "미확인 항목 — 운영팀 직접 확인 필요" → "사전 협의 권장 항목"(amber→slate).
  5. **미확인 항목 구체화** — `getGuideUnknowns()`가 install_allowed에서 카테고리명 직접 추출. 매뉴얼 OCR/내부 처리 메시지 제거.
  6. **원문 보존** — venueFacilityGuide.ts는 이미 시드 원문. AI 의역 금지 원칙 유지.

**대안**:
  - 호버 박스 max-width만 늘리기 (회의 의도 — "두 줄로 작성"과 부분 정합) → 행 자체를 wrap으로 바꾸는 게 더 안정적
  - 미확인 항목 섹션 전체 삭제 → 구체 리스트업 가능한 케이스는 유지가 낫다고 판단
  - AI 텍스트 가공을 추천 결과 컴포넌트마다 분산 → 단일 유틸로 한 곳에서 관리

**이유**:
  - 사용자 인용은 "사용자가 보는 객관 정보(코엑스 등 1차 자료)는 잘림 없이 전부 보여야 한다"는 메모리 [[feedback-facility-guide-full-visibility]] 원칙과 완전 일치
  - pointer-events 분리는 React 모달 패턴 표준 — bubble 충돌 회피
  - normalizeAiText 유틸 신설로 향후 AI 텍스트 노출 컴포넌트 추가 시 일관 적용

**되돌릴 수 있는가**: 쉬움 — 각 변경이 독립적. install_allowed wrap은 className 1줄, pointer-events는 outer div 1곳, formatNoteText는 import 제거만으로 원복.

**상세**: PROGRESS.md 2026-05-13 (v9.21) 섹션 / 회의록 `회의록_환경장식물_시설가이드_피드백_260513.md`

## 2026-05-12 — 제작 완료 데이터 > 시설 가이드 규칙 (신뢰도 계층)

**컨텍스트**: 사용자 요청 — "제작 완료(PPT·엑셀 다운로드 시) 데이터가 가장 중요한 정보. 알랏 무시하고 완료한 케이스도 취합. 기존 가이드 데이터가 틀릴 수 있다."
**선택**:
  1. **PPT·엑셀 모두 finalized_at** — 두 다운로드 모두 제작 완료로 간주 (`ExportService.logUsage` 통합)
  2. **exception_log + finalized = 가이드 오류 신호** — 알랏 무시 후 실제 완료된 케이스는 "현장에서 검증된 실제 값"으로 처리. `accumulatedContext`에 exception 패턴 포함.
  3. **venue+rule 3회 이상 예외 → 가이드 검토 플래그** — `exception-monitor` API + 학습 관리자 UI 표시. "가이드 검토 필요" 배지.
  4. **신뢰도 계층**: 제작 완료(100%) > 사용자 컨펌(70%) > 중간 수정(30%) > 시설 가이드 종이 매뉴얼 (이미 오래됐을 수 있음)
**대안**:
  - 시설 가이드를 항상 최고 권위로 유지 (틀려도 강제)
  - 예외 케이스를 학습에서 제외 (패턴 정보 손실)
**이유**: 시설 가이드 데이터는 수동으로 입력된 것이라 오래될 수 있음. 실제 행사에서 완료된 데이터가 더 신뢰할 수 있는 현재 상태를 반영함. 가이드는 "처음 접하는 사람을 위한 안내"이고, 완료 데이터는 "실제로 한 것"임.
**되돌릴 수 있는가**: 쉬움 — `STAGE_WEIGHT`에서 finalized=100으로 고정한 것만 유지하면 됨. exception 패턴 Context는 별도이므로 `accumulatedContext.ts`의 exception_patterns 블록만 제거하면 원래대로.
